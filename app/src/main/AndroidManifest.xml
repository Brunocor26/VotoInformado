<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VotoInformado"
        tools:targetApi="31">

        <activity
            android:name=".activities.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.DevActivity"
            android:parentActivityName=".activities.SettingsActivity" />
        <activity
            android:name=".activities.AddCandidateActivity"
            android:parentActivityName=".activities.DevActivity" />
        <activity
            android:name=".activities.AddPollActivity"
            android:parentActivityName=".activities.DevActivity" />
        <activity
            android:name=".activities.AddDateActivity"
            android:parentActivityName=".activities.DevActivity" />

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.SettingsActivity" />

        <activity
            android:name=".activities.NoticiaDetalheActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".activities.CandidatoDetailActivity"
            android:parentActivityName=".activities.HomeActivity" />
        <activity
            android:name=".activities.SondagemDetailActivity"
            android:parentActivityName=".activities.HomeActivity" />
        <activity
            android:name=".activities.ImportantDatesActivity"
            android:parentActivityName=".activities.HomeActivity" />

        <activity
            android:name=".activities.CreatePeticaoActivity"
            android:parentActivityName=".activities.HomeActivity" />
        <activity
            android:name=".activities.PeticaoDetailActivity"
            android:parentActivityName=".activities.HomeActivity" />
            android:name=".activities.PoliticalCompassActivity"
            android:parentActivityName=".activities.HomeActivity" />

        <activity
            android:name=".activities.PoliticalCompassResultActivity"
            android:parentActivityName=".activities.PoliticalCompassActivity" />

    </application>

</manifest>